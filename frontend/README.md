# AE-MAP Frontend

Next.js dashboard for running and inspecting the AE-MAP pipeline.

## Run

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` to point at the FastAPI service when it is not running at `http://localhost:8000/api/v1`.
